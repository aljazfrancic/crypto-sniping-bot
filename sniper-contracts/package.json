{
  "name": "sniper-contracts",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "clean": "hardhat clean",
    "test": "hardhat test",
    "deploy:lpchecker": "hardhat run scripts/deploy-lpchecker.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "dotenv": "^16.4.0",
    "hardhat": "^2.22.0"
  },
  "resolutions": {
    "glob": "^9.3.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.3.0"
  }
}
