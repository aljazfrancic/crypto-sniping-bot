{
  "dependencies": {
    "@openzeppelin/contracts": "^5.3.0",
    "@truffle/hdwallet-provider": "^1.7.0",
    "crypto-js": "^4.1.1",
    "keytar": "^7.9.0"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0"
  },
  "scripts": {
    "test": "npm run test:contracts && npm run test:app",
    "build": "tsc",
    "test:contracts": "cd sniper-contracts && npm run test",
    "test:app": "jest --coverage --testPathIgnorePatterns=sniper-contracts"
  }
}